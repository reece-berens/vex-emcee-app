/**
 * Footer Component
 *
 * Legacy footer wrapper - currently empty.
 * The actual footer content (BottomNav) is now passed directly
 * to AppWrapper as a prop to keep it outside the scroll area.
 *
 * This component may be removed or repurposed in the future.
 */

export default function Footer() {
	return <footer className="layout-footer"></footer>;
}
